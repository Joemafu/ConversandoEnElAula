import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'conversando.en.el.aula',
  appName: 'ConversandoEnElAula',
  webDir: 'www'
};

export default config;
